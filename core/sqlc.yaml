version: "2"
sql:
  - engine: "postgresql"
    queries: "storage/postgres/query"
    schema: "storage/postgres/migration"
    gen:
      go:
        package: "postgres"
        out: "storage/postgres"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
